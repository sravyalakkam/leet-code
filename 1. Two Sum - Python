class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        hash_set = {}
        for i,n in enumerate(nums):
            if (target-n) in hash_set:
                return [hash_set[target-n],i]
            hash_set[n] = i
        return [-1,-1]
            
